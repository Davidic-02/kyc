import 'package:flutter/material.dart';

class AppColors {
  static const primaryPurple = Color(
    0xFFC0A9FF,
  ); // The light purple in the "Log In" button
  static const primaryBlue = Color(0xFF7B61FF); // The deeper purple/blue glow
  static const accentGlow = Color(
    0xFFE2D9FF,
  ); // The brightest part of the radial glow

  // Backgrounds (Dark Mode)
  static const darkBackground = Color(
    0xFF0D0B1E,
  ); // The deep navy/black background
  static const surfaceDark = Color(
    0xFF1B1836,
  ); // Slightly lighter for card-like surfaces

  // Interactive Elements
  static const buttonSecondary = Color(0xFF221F3D); // The dark "Sign Up" button
  static const inputBackground = Color(0xFF16142E); // Input field background

  // Text
  static const textWhite = Color(0xFFFFFFFF);
  static const textMuted = Color(0xFFA6A2C2); // The grayish-purple subtext
}
