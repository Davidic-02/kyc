import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Branding & Gradients
  static const Color primary = Color(0xFF7B61FF); // Deep Purple
  static const Color accent = Color(
    0xFFC0A9FF,
  ); // Light Glow Purple (The Button)
  static const Color highlight = Color(
    0xFFE2D9FF,
  ); // Near White Purple (Gradient Start)

  // Backgrounds
  static const Color background = Color(0xFF0D0B1E); // Deep Navy/Black
  static const Color surface = Color(0xFF1B1836); // Card Surface / Inputs

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA6A2C2); // Muted Purple-Grey
}
